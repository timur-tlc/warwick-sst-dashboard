# Streamlit Secrets Configuration
# Copy this file to secrets.toml for local development
# For Streamlit Cloud, add these in the app settings > Secrets

[aws]
access_key_id = "AKIAXXXXXXXXXXXXXXXXX"
secret_access_key = "your-secret-access-key-here"

# To get actual credentials, run:
# cd clients/warwick.com.au/opentofu
# AWS_PROFILE=warwick tofu output -raw streamlit_secrets_config
